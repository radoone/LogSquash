{
  "name": "logsquash-mcp",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/index.js",
    "start": "node dist/index.js",
    "version": "node -e \"let fs=require('fs'); let ext=require('./gemini-extension.json'); ext.version=process.env.npm_package_version; fs.writeFileSync('./gemini-extension.json', JSON.stringify(ext, null, 2) + '\\n');\" && git add gemini-extension.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "esbuild": "^0.28.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
